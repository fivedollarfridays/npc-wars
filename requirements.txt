# NPC Wars — runtime dependencies
# Engine uses Python stdlib only (no third-party runtime deps yet)

# Dev / CI dependencies
pytest>=9.0
pytest-cov>=7.0
pytest-asyncio>=0.23
ruff>=0.14
mypy>=1.19
pyyaml>=6.0

# S3: Discord bot
discord.py>=2.3

# S4: Video renderer
Pillow>=10.0
# google-api-python-client>=2.0  # S5: YouTube upload
