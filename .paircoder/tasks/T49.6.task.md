---
id: T49.6
title: 'GATE: Security regression suite'
plan: plan-2026-03-s49-security-hardening
type: chore
priority: P1
complexity: 15
status: pending
sprint: S49
tags: [security, integration, gate]
depends_on: [T49.1, T49.2, T49.3, T49.4, T49.5]
---

# Objective

Integration gate proving all security fixes work together and no regressions exist.

# Implementation Plan

1. Create `tests/test_s49_integration.py`:

   **TestSandboxSecurity**
   - Bot with `__import__("os")` is rejected by scanner
   - Bot with `getattr((), '__class__')` is rejected by scanner
   - Bot with `vars()` is rejected by scanner
   - All example bots in `bots/` still pass validation
   - Sandbox exec doesn't expose __import__ in builtins

   **TestXSSProtection**
   - All HTML pages (tournaments, leaderboard, profile, editor, store) contain sanitize function
   - No raw innerHTML usage with user-controlled fields (grep for patterns)

   **TestServerHardening**
   - Rate limiter blocks rapid submissions from same player
   - Stats route returns 400 on invalid sort_by
   - CORS preflight only allows GET/POST
   - API key lookup works (hash-based)
   - Share route rejects path traversal
   - Match route rejects path traversal (existing, verify still works)

2. Run full test suite — verify no regressions
3. Run ruff check

# Acceptance Criteria

- [ ] Sandbox escape attempts blocked
- [ ] XSS payloads escaped in all pages
- [ ] Rate limiter not bypassable
- [ ] Server middleware hardened
- [ ] API keys not stored plaintext
- [ ] All tests pass, ruff clean
- [ ] No regressions in existing 4100+ tests
