---
id: T43.4
title: 'GATE: Phase 3A completion — full product loop'
plan: plan-2026-03-s43-leaderboard
type: chore
priority: P1
complexity: 15
status: pending
sprint: S43
tags: [integration, gate]
depends_on: [T43.1, T43.2, T43.3]
---

# Objective

Phase 3A completion gate. Validate the full product loop: install → create bot → upload → fight → see rank → get results.

# Implementation Plan

1. Create `tests/test_s43_integration.py`:

   **TestLeaderboardWeb**
   - GET /leaderboard returns HTML
   - GET /api/leaderboard returns JSON with ranked players
   - Players sorted correctly

   **TestProfileWeb**
   - GET /profile/{id} returns HTML
   - GET /api/stats/{id} returns player stats

   **TestDiscordAnnouncement**
   - Match completion triggers announcement function
   - Announcement data has correct fields (winner, kills, replay link)

   **TestCLILeaderboard**
   - CLI leaderboard command registered
   - Handles server unavailable gracefully

   **TestPhase3ALoop**
   - pip install works (S40)
   - Viewer renders terrain + shapes (S41)
   - Server accepts bot upload (S42)
   - Leaderboard shows results (S43)
   - Full loop validated

2. Update docs/road-to-completion.md: mark Phase 3A complete

# Acceptance Criteria

- [ ] Web leaderboard accessible
- [ ] Web profiles accessible
- [ ] Discord announcements functional
- [ ] CLI leaderboard works
- [ ] Phase 3A documented as complete
