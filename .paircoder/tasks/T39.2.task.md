---
id: T39.2
title: 'Feed.py refactor — extract formatters'
plan: plan-2026-03-s39-phase2-gate
type: refactor
priority: P1
complexity: 20
status: pending
sprint: S39
tags: [refactor, architecture]
depends_on: []
---

# Objective

Refactor `agentgrounds/wars/cli/feed.py` to fix the 28-function architecture violation (limit: 15). Extract formatter functions into a data-driven registry.

# Implementation Plan

1. Create `agentgrounds/wars/cli/feed_formatters.py`:
   - Move all `_fmt_*` functions here
   - Export `FORMATTERS` dict mapping event type → formatter function
   - Each formatter: `(evt: dict, rnd: int) -> str`

2. Slim `agentgrounds/wars/cli/feed.py`:
   - Keep only `format_feed_event()` as the public API
   - Import `FORMATTERS` from feed_formatters
   - Target: < 15 functions, < 100 lines

3. All existing tests must pass unchanged (pure refactor)

# Acceptance Criteria

- [ ] feed.py under 15 functions
- [ ] All feed formatting tests pass unchanged
- [ ] No behavior changes
- [ ] ruff clean
