---
id: T71.1
title: Extract viewer.html inline CSS
plan: plan-sprint-71-engage
type: feature
priority: P0
complexity: 15
status: done
sprint: '71'
depends_on: []
completed_at: '2026-04-16T08:36:51.277374'
---

# Extract viewer.html inline CSS

`viewer/viewer.html` is 1002 lines, 812 of which are inline CSS. This exceeds the 400-line source limit. The JS is already properly split into `viewer/js/*.js` modules; the CSS should follow the same pattern. Move the entire `<style>` block content to a new `viewer/viewer.css`, replace the inline block with `<link rel="stylesheet" href="viewer.css">`. No behavior changes — pure extraction. Verify all existing viewer tests still pass and the page renders identically.

# Acceptance Criteria

- [x] `viewer/viewer.css` exists containing all CSS that was previously inline
- [x] `viewer/viewer.html` under 400 lines after extraction
- [x] `<link rel="stylesheet" href="viewer.css">` replaces the inline `<style>` block
- [x] All existing viewer tests pass (test_viewer_*, test_viewer_unified, test_viewer_code_overlay, test_viewer_commentary, test_viewer_circuit)
- [x] Visual diff snapshot or manual smoke: viewer loads a sample match JSON and renders without CSS regressions
- [x] `bpsai-pair arch check viewer/viewer.html` passes
- [x] No behavioral change — pure refactor