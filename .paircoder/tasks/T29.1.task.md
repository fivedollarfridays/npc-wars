---
id: T29.1
title: 'Extract combat helpers from rounds.py'
plan: plan-2026-03-s29-combat-pt2
type: refactor
priority: P1
complexity: 20
status: pending
sprint: S29
tags: [engine, architecture]
depends_on: []
---

# Objective

rounds.py is at 390 LOC / 16 functions (over 15-function limit). Extract attack resolution functions into a new `engine/rounds_combat.py` module.

# Functions to Move

- `resolve_attacks()` + `_roll_melee()` + `_find_target()` + `build_pos_map()`
- `resolve_ranged_attacks()` + `_roll_ranged()`

# Keep in rounds.py

- `resolve_decisions()`, `resolve_defense()`, `resolve_movement()`, `resolve_taunt()`
- `apply_storm_damage()`, `apply_energy_and_rest()`, `attribute_kills()`, `build_round_record()`

# After extraction

- rounds.py re-exports `resolve_attacks`, `resolve_ranged_attacks`, `build_pos_map` for backward compat
- game.py imports stay the same (from engine.rounds)
- All existing tests pass without changes

# Acceptance Criteria

- [ ] rounds_combat.py contains attack resolution functions
- [ ] rounds.py drops to < 15 functions and < 350 LOC
- [ ] All existing tests pass unchanged
- [ ] ruff clean
