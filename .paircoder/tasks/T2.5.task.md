---
id: T2.5
title: Add requirements.txt and pyproject.toml
plan: plan-2026-03-s2-data-ci
type: chore
priority: P1
complexity: 10
status: pending
sprint: S2
tags: []
depends_on: []
---

# Objective
Add requirements.txt and pyproject.toml for proper Python project setup.

# Implementation Plan
- Create pyproject.toml with project metadata, dependencies, dev-dependencies
- Create requirements.txt (pinned versions) for engine (stdlib only currently)
- Dev deps: pytest, pytest-cov, ruff, mypy
- Future deps placeholder: discord.py, Pillow, google-api-python-client
- Configure pytest in pyproject.toml: testpaths, coverage settings
- Configure ruff in pyproject.toml: line length, target python version

# Acceptance Criteria
- [ ] pyproject.toml exists with valid project metadata
- [ ] requirements.txt exists (even if minimal)
- [ ] Dev dependencies installable: `pip install -e ".[dev]"`
- [ ] pytest runs from pyproject.toml config
- [ ] ruff lint passes on existing code
- [ ] All tests pass after setup

# Verification
- `pip install -e ".[dev]"` — succeeds
- `pytest` — runs from config
- `ruff check .` — passes or only warnings
