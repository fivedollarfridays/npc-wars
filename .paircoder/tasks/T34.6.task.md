---
id: T34.6
title: 'GATE: trap polish integration test'
plan: plan-2026-03-s34-trap-polish
type: chore
priority: P1
complexity: 20
status: pending
sprint: S34
tags: [integration, gate]
depends_on: [T34.1, T34.2, T34.3, T34.4, T34.5]
---

# Objective

Integration gate for S34. Verify full trap visibility, on_kill callback, and balance pipeline end-to-end.

# Implementation Plan

1. Create `tests/test_s34_integration.py` with test classes:

   **TestTrapFeedRendering**
   - Run match with trapper bot, verify trap events appear in formatted feed
   - trap_placed and trap_trigger events format correctly

   **TestOnKillCallback**
   - Run match where bot kills another, verify on_kill fires
   - on_kill receives correct victim emoji
   - Storm kills don't trigger on_kill

   **TestTrapperBot**
   - Load example_trapper.py, verify it passes security scan
   - Run match with trapper, verify it places traps
   - Trapper uses setup() and react() callbacks

   **TestStateDict**
   - state["enemies"][i]["trap_count"] populated correctly
   - state["me"]["traps"] accurate during match

   **TestNoDeadCode**
   - All public functions in callback_runner.py have callers
   - All feed formatters registered
   - on_kill callback wired into game loop

2. Verify no regressions across full test suite

# Acceptance Criteria

- [ ] Integration tests verify full trap visibility pipeline
- [ ] on_kill callback verified in real match flow
- [ ] Trapper bot loads and functions
- [ ] State dict fields accurate
- [ ] No dead code in new/modified modules
- [ ] No regressions, ruff clean
