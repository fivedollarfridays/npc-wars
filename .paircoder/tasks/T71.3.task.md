---
id: T71.3
title: Extract engine/rounds.py helpers to reduce file size
plan: plan-sprint-71-engage
type: feature
priority: P0
complexity: 20
status: done
sprint: '71'
depends_on: []
---

# Extract engine/rounds.py helpers to reduce file size

`engine/rounds.py` is exactly 350 lines, at the test's `<= 350` boundary. The next line-adding change breaks CI. Extract natural helper groups into a companion module (e.g., `engine/rounds_helpers.py` or split by concern into `engine/action_resolution.py` + `engine/override_events.py`). Target: `rounds.py` under 300 lines after extraction, helpers under 200 each. Preserve `resolve_decisions()` as the public entry point; all callers import from `engine.rounds` as before. Include a size-guard test update to reflect the new limit.

# Acceptance Criteria

- [x] `engine/rounds.py` under 300 lines after extraction (now 177)
- [x] One or more helper modules created, each under 200 lines (rounds_decisions.py 96, rounds_movement.py 127)
- [x] Public entry point `resolve_decisions` still importable from `engine.rounds`
- [x] `tests/test_s29_integration.py::TestArchCompliance::test_rounds_py_arch_compliant` passes with updated assertion (`<= 200`)
- [x] All existing engine and rounds tests pass
- [x] `bpsai-pair arch check engine/rounds.py` passes
- [x] `bpsai-pair arch check <new-helper-files>` passes
- [x] No behavioral change — pure refactor