---
id: T71.4
title: Fix server/lobby.py fire-and-forget thread
plan: plan-sprint-71-engage
type: feature
priority: P1
complexity: 10
status: done
sprint: '71'
depends_on: []
---

# Fix server/lobby.py fire-and-forget thread

`server/lobby.py::_run_match_inline` spawns a thread with no join, no result tracking, errors only logged. The S57 change that added this path meant well (inline execution in in-memory mode so local dev works without Redis) but it swallows errors silently, and it's the same change that broke 10+ tests this sprint. Two options: (a) add a `LOBBY_INLINE_ON_NO_REDIS` config flag (default `False`) so production and tests use the queue path, and local dev opts in; or (b) run synchronously in the fallback path — callers wait but get structured error feedback. Pick (a) — it's less disruptive, matches the principle of explicit opt-in, and tests stop needing the `NotInMemoryQueue` helper in `tests/conftest.py`.

# Acceptance Criteria

- [x] New env var `LOBBY_INLINE_ON_NO_REDIS` read in `server/lobby.py` (default `False`)
- [x] Inline execution only happens when the flag is `True` AND `is_in_memory_mode()` is `True`
- [x] When flag is `False` (default), lobby always uses `enqueue_match()` regardless of backend
- [x] `tests/conftest.py::NotInMemoryQueue` helper removed (no longer needed)
- [x] `tests/test_lobby.py`, `test_extended_mode.py`, `test_server_integration.py`, `test_s52_lobby_rival.py` updated to not use `NotInMemoryQueue` (InMemoryQueue works directly now since flag defaults False)
- [x] `_run_match_inline` kept but documented as opt-in; still logs errors structurally
- [x] Documentation updated: README or CONTRIBUTING notes the env var for local dev convenience
- [x] `bpsai-pair arch check` clean
- [x] All server/lobby tests pass