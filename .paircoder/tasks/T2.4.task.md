---
id: T2.4
title: Create scripts/validate_bot.py (syntax, attrs, sandbox test)
plan: plan-2026-03-s2-data-ci
type: feature
priority: P1
complexity: 30
status: pending
sprint: S2
tags: []
depends_on: []
---

# Objective
Create scripts/validate_bot.py — standalone bot validator for CI and local use.

# Implementation Plan
- Create scripts/validate_bot.py (runnable: python scripts/validate_bot.py bots/my_bot.py)
- Checks: syntax valid, required attrs (BOT_NAME, BOT_EMOJI, decide), callable decide
- Sandbox test: run decide() with mock state, verify returns valid action tuple
- Timeout test: decide() completes within 1 second
- Exit codes: 0=pass, 1=fail with detailed error messages
- Reuse engine/sandbox.py validate_action()

# Acceptance Criteria
- [ ] scripts/validate_bot.py exists and runs standalone
- [ ] Validates syntax, required attributes, callable decide
- [ ] Runs sandbox test with mock state
- [ ] Timeout enforcement (1 second)
- [ ] Clear error messages on failure
- [ ] Tests in tests/test_validate_bot.py with ≥8 cases
- [ ] All tests pass

# Verification
- `python scripts/validate_bot.py bots/example_aggro.py` — exits 0
- `pytest tests/test_validate_bot.py -v` — all green
