---
id: T2
title: Test PyPI publish
plan: plan-2026-03-npc-wars-get-sellable
type: chore
priority: P1
complexity: 30
status: cancelled
sprint: S19
tags: []
depends_on:
- T1
---

# Objective

Publish to TestPyPI and verify the package installs and runs correctly from the test index.

# Implementation Plan

1. Upload distribution to TestPyPI: `twine upload --repository testpypi dist/*`
2. Create a clean venv and install: `pip install --index-url https://test.pypi.org/simple/ npc-wars`
3. Verify CLI works: `npcwars init && npcwars battle` produces output

# Acceptance Criteria

- [ ] Package uploaded to TestPyPI without errors
- [ ] Package installs from TestPyPI in a clean venv
- [ ] `npcwars init && npcwars battle` produces output

# Verification

```bash
pip install --index-url https://test.pypi.org/simple/ npc-wars
npcwars init && npcwars battle
```
