---
id: T38.4
title: 'Game.py refactor — extract match phases'
plan: plan-2026-03-s38-post-match
type: refactor
priority: P1
complexity: 20
status: pending
sprint: S38
tags: [engine, refactor, architecture]
depends_on: []
---

# Objective

Refactor `engine/game.py` to fix the 408+ line architecture violation. Extract match phase helpers into separate modules.

# Implementation Plan

1. Extract `_resolve_combat_phases()` and related helpers into `engine/match_phases.py`:
   - `_resolve_combat_phases()` — the big combat resolution function
   - `_apply_plague_phase()` — plague logic
   - `_apply_momentum_phase()` — scoring + momentum
   - `_score_spectacle()` — spectacle engine

2. Extract `_create_bots()` and `_prepare_match()` into `engine/match_setup.py`:
   - Bot creation with equipment, callbacks, terrain wiring
   - Match preparation (grid, spawns, RNG)

3. Keep `run_match()` and `_execute_round()` in game.py as the orchestrator

4. Verify game.py drops below 400 lines (target: ~200)

5. All existing tests must pass unchanged (pure refactor)

# Acceptance Criteria

- [ ] game.py under 400 lines (ideally under 250)
- [ ] No behavior changes — pure refactor
- [ ] All existing tests pass
- [ ] New modules have `__all__` exports
- [ ] ruff clean
