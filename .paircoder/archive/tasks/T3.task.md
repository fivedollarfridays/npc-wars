---
id: T3
title: Publish to PyPI
plan: plan-2026-03-npc-wars-get-sellable
type: chore
priority: P1
complexity: 15
status: cancelled
sprint: S19
tags: []
depends_on:
- T2
---

# Objective

Publish the package to production PyPI and verify it installs and runs from the public index.

# Implementation Plan

1. Upload distribution to PyPI: `twine upload dist/*`
2. Create a clean venv and install: `pip install npc-wars`
3. Verify CLI works: `npcwars init && npcwars battle` produces output

# Acceptance Criteria

- [ ] Package uploaded to PyPI without errors
- [ ] `pip install npc-wars` works in a clean venv
- [ ] `npcwars init && npcwars battle` works after install

# Verification

```bash
pip install npc-wars
npcwars init && npcwars battle
```
